@model IEnumerable<KT100720_59132942.Models.LoaiT>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLTS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLTS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLTS }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLTS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLTS })
        </td>
    </tr>
}

</table>
